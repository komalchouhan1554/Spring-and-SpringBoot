# 🌐 Servlet Notes

## ❓ What is Servlet?

1. 🖥️ Servlet is a **"Web Technology"** which is used to create dynamic web applications.
2. 📦 Servlet is a **"Web Container"** which is used to process the dynamic requests (Servlet Container).
3. 📚 Servlet is an **"API"** (which contains classes, interfaces, and documentation).
4. 🧩 Servlet is a **"Pre-defined Interface"**.
5. 🏗️ Servlet is a **"Pre-defined Class"**.

---

## 📦 Servlet API

It contains 2 main packages:

1. 📁 `javax.servlet` package
2. 📁 `javax.servlet.http` package

---

## 🛠️ How to Create a Servlet

There are 3 ways to create a Servlet:

1. ✅ By implementing the **`Servlet`** interface
2. ✅ By extending the **`GenericServlet`** class
3. ✅ By extending the **`HttpServlet`** class

### 📝 NOTE

> 💡 We mostly create a servlet by extending the **`HttpServlet`** class because it follows the **HTTP protocol** ⚡ (which is important in web development).